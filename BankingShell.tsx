import { useEffect, useMemo, useState } from 'react'
import { Activity, Bell, CreditCard, Home, Landmark, LogOut, Menu, MessageSquare, MoveRight, Receipt, Search, Settings, ShieldCheck, Users, X } from 'lucide-react'
import { NavLink, useLocation, useNavigate } from 'react-router-dom'
import { useCustomerData } from './CustomerProvider'
import { FNCUWordmark } from './FNCUBrand'
import './world-v2.css'
import './ux-audit.css'
import './fncu-identity.css'
import './fncu-design-system.css'
import './fncu-navigation-fixes.css'

const money=(n:number)=>`${n<0?'-':''}$${Math.abs(n).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}`
const nav=[['Home','/',Home],['Activity','/transactions',Activity],['Move','/transfers',MoveRight],['Profile','/profile',Settings]] as const
type DrawerItem={label:string;path:string;icon:any};type DrawerSection={label:string;items:DrawerItem[]}

export function BankingShell({children,showHeader=true}:{children:React.ReactNode;showHeader?:boolean}){
 const[open,setOpen]=useState(false);const[searchOpen,setSearchOpen]=useState(false);const[query,setQuery]=useState('');const{profile,account,transactions}=useCustomerData();const navigate=useNavigate();const location=useLocation()
 const matches=useMemo(()=>transactions.filter((t:any)=>`${t.description??''} ${t.counterparty??''} ${t.reference??''}`.toLowerCase().includes(query.toLowerCase())).slice(0,5),[transactions,query])
 const name=profile?.preferred_name||profile?.full_name||'Customer';const initials=name.split(/\s+/).filter(Boolean).map((p:string)=>p[0]).join('').slice(0,2).toUpperCase()||'CU';const avatarUrl=profile?.avatar_url||''
 const Avatar=()=>avatarUrl?<img className="fncu-avatar-image" src={avatarUrl} alt="" aria-hidden="true"/>:<span>{initials}</span>
 const isActive=(path:string)=>path==='/'?location.pathname==='/':location.pathname.startsWith(path)
 const drawerActive=(path:string)=>{const [pathname,search]=path.split('?');if(location.pathname!==pathname)return false;if(!search)return pathname==='/'?location.pathname==='/':true;return new URLSearchParams(location.search).toString()===search}
 useEffect(()=>{if(!open)return;const onKey=(event:KeyboardEvent)=>{if(event.key==='Escape')setOpen(false)};document.addEventListener('keydown',onKey);if(window.matchMedia('(max-width: 900px)').matches)document.body.style.overflow='hidden';return()=>{document.removeEventListener('keydown',onKey);document.body.style.overflow=''}},[open])
 useEffect(()=>{setOpen(false);setSearchOpen(false)},[location.pathname,location.search])
 const sections:DrawerSection[]=[
  {label:'Transfers',items:[{label:'Transfer money',path:'/transfers',icon:MoveRight},{label:'Transfer history',path:'/transactions',icon:Activity}]},
  {label:'Payments & deposits',items:[{label:'Pay a person',path:'/transfers?mode=p2p',icon:Users},{label:'Internal transfer',path:'/transfers?mode=internal',icon:Landmark},{label:'ACH transfer',path:'/transfers?mode=ach',icon:MoveRight},{label:'Remote check deposit',path:'/deposit',icon:Receipt},{label:'Pay a bill',path:'/bills',icon:Receipt}]},
  {label:'Account',items:[{label:'Alerts',path:'/messages?view=alerts',icon:Bell},{label:'Secure messages',path:'/messages',icon:MessageSquare}]},
  {label:'Cards',items:[{label:'Debit card',path:'/cards',icon:CreditCard}]},
  {label:'Profile & security',items:[{label:'Profile & settings',path:'/profile',icon:Settings},{label:'Beneficiaries',path:'/beneficiaries',icon:Users}]},
  {label:'Support',items:[{label:'Customer service',path:'/messages?view=customer-service',icon:MessageSquare}]},
 ]
 return <div className="app-shell fncu-reference-app"><div className="fncu-reference-device">{showHeader&&<header className="fncu-mobile-header"><button className="fncu-header-icon fncu-menu-button" onClick={()=>setOpen(v=>!v)} aria-label={open?'Close navigation':'Open navigation'} aria-expanded={open}><Menu size={19}/></button><FNCUWordmark compact/><div className="fncu-header-tools"><button className="fncu-header-icon" onClick={()=>setSearchOpen(v=>!v)} aria-label="Search activity"><Search size={18}/></button><NavLink className="fncu-header-icon" to="/messages?view=alerts" aria-label="Alerts"><Bell size={18}/></NavLink><button className="fncu-header-profile" onClick={()=>navigate('/profile')} aria-label="Open profile"><span className="avatar"><Avatar/></span></button></div></header>}{showHeader&&searchOpen&&<div className="fncu-search-panel"><div className="fncu-search-box"><Search size={17}/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search your banking activity"/><button onClick={()=>{setQuery('');setSearchOpen(false)}} aria-label="Close search"><X size={17}/></button></div>{query&&<div className="fncu-search-results">{matches.length?matches.map((t:any)=><button key={t.id} onClick={()=>{setQuery('');setSearchOpen(false);navigate('/transactions')}}><span>{t.description||t.counterparty||'Account activity'}</span><b>{money((t.direction==='debit'?-1:1)*Number(t.amount))}</b><small>{t.date} · {t.reference||''}</small></button>):<p>No matching activity.</p>}</div>}</div>}<main className="content fncu-reference-content">{children}</main><nav className="mobile-nav fncu-reference-nav" aria-label="Primary navigation">{nav.map(([label,path,Icon])=><NavLink key={label} to={path} end={path==='/' } className={()=>isActive(path)?'active':''}><Icon size={19}/><span>{label}</span></NavLink>)}</nav></div><aside className={`fncu-reference-drawer${open?' open':''}`} aria-hidden={!open}><div className="fncu-drawer-head"><FNCUWordmark compact/><button className="fncu-header-icon fncu-drawer-close" onClick={()=>setOpen(false)} aria-label="Close navigation"><X size={18}/></button></div><button className="fncu-drawer-account" onClick={()=>{setOpen(false);navigate('/profile')}}><span className="avatar"><Avatar/></span><span><b>{name}</b><small>{account?.account_type||'Checking'} · {account?.currency||'USD'}</small></span></button><div className="fncu-drawer-links"><NavLink to="/" end onClick={()=>setOpen(false)} className={isActive('/')?'active':''}><Home size={18}/><span>Overview</span></NavLink>{sections.map(section=><div className="fncu-drawer-section" key={section.label}>{section.items.length>0&&<div className="fncu-drawer-section-label">{section.label}</div>}{section.items.map(item=><NavLink key={`${section.label}-${item.label}`} to={item.path} onClick={()=>setOpen(false)} className={drawerActive(item.path)?'active':''}><item.icon size={17}/><span>{item.label}</span></NavLink>)}</div>)}</div><div className="fncu-drawer-footer"><div className="fncu-drawer-security"><ShieldCheck size={15}/><span>Secure customer access</span></div><button className="fncu-drawer-signout" onClick={()=>void import('./supabaseClient').then(({supabase})=>supabase.auth.signOut())}><LogOut size={16}/>Sign out</button></div></aside>{open&&<button className="fncu-drawer-backdrop" onClick={()=>setOpen(false)} aria-label="Close navigation"/>}</div>
}

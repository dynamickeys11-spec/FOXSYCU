import { useMemo, useState } from 'react'
import { Bell, CalendarDays, MessageSquare, Search, ShieldCheck, X } from 'lucide-react'
import { BankingShell } from './BankingShell'
import { useCustomerData } from './CustomerProvider'
import './feature-banking.css'
const fmt=(v:any)=>{if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})}
export default function MessagesPage(){const{messages,transactions}=useCustomerData();const[q,setQ]=useState('');const[open,setOpen]=useState<any|null>(null);const txMap=useMemo(()=>new Map(transactions.map(t=>[t.reference,t])),[transactions]);const rows=messages.filter((m:any)=>`${m.subject||''} ${m.body||''} ${m.transaction_reference||''}`.toLowerCase().includes(q.toLowerCase()));const linked=(m:any)=>m.transaction_reference?txMap.get(m.transaction_reference):undefined;return <BankingShell><div className="fb-page"><header className="fb-heading"><span>SECURE COMMUNICATION</span><h1>Messages</h1><p>Transaction alerts and service notices are dated against the banking event they describe.</p></header><div className="fb-card" style={{marginBottom:14}}><label style={{display:'flex',alignItems:'center',gap:8,border:'1px solid #E4E7EC',borderRadius:8,padding:'0 11px',height:38}}><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search messages" style={{border:0,outline:0,width:'100%',fontSize:12}}/></label></div><section className="fb-card"><div className="fb-card-head"><div><h2>Inbox</h2><p>{rows.length} message{rows.length===1?'':'s'} · transaction-linked notices use transaction effective/posted dates</p></div></div><div>{rows.map((m:any)=>{const t=linked(m);const eventDate=t?.date||m.event_at||m.created_at;return <button key={m.id} type="button" onClick={()=>setOpen(m)} style={{width:'100%',border:0,borderTop:'1px solid #F0F2F5',background:'#fff',padding:'14px 0',display:'grid',gridTemplateColumns:'34px minmax(0,1fr) auto',gap:12,alignItems:'start',textAlign:'left',cursor:'pointer'}}><span style={{width:32,height:32,borderRadius:8,display:'grid',placeItems:'center',background:m.priority==='urgent'?'#FFF1F0':'#EFF4FF',color:m.priority==='urgent'?'#B42318':'#155EEF'}}>{m.message_type==='security'?<ShieldCheck size={15}/>:m.transaction_reference?<Bell size={15}/>:<MessageSquare size={15}/>}</span><span style={{minWidth:0}}><b style={{display:'block',fontSize:12,color:'#101828'}}>{m.subject}</b><strong style={{display:'block',marginTop:4,fontSize:10,lineHeight:1.45,color:'#667085',fontWeight:500,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{m.body}</strong><small style={{display:'block',marginTop:6,color:'#98A2B3',fontSize:9}}>{t?`${t.description} · ${t.reference}`:m.message_type||'Service message'}</small></span><span style={{fontSize:9,color:'#667085',whiteSpace:'nowrap'}}>{fmt(eventDate)}{!m.read_at&&<i style={{display:'inline-block',marginLeft:7,width:6,height:6,borderRadius:'50%',background:'#155EEF'}}/>}</span></button>})}</div></section>{open&&<div className="drawer-backdrop" onClick={()=>setOpen(null)}><aside className="drawer" onClick={e=>e.stopPropagation()} style={{maxWidth:560}}><div className="drawer-head"><div><small>SECURE MESSAGE</small><h2>{open.subject}</h2></div><button className="icon-button" onClick={()=>setOpen(null)}><X size={18}/></button></div><p style={{fontSize:12,lineHeight:1.6,color:'#475467'}}>{open.body}</p>{linked(open)&&<div className="drawer-details"><div><span>Transaction date</span><b>{fmt(linked(open)?.date)}</b></div><div><span>Transaction</span><b>{linked(open)?.description}</b></div><div><span>Amount</span><b>{linked(open)?.amount>=0?'+':''}${Math.abs(Number(linked(open)?.amount||0)).toLocaleString('en-US',{minimumFractionDigits:2})}</b></div><div><span>Payment rail</span><b>{String(linked(open)?.metadata?.payment_rail||'').replace('_',' ')||'—'}</b></div><div><span>Reference</span><b>{linked(open)?.reference||open.transaction_reference}</b></div><div><span>Institution</span><b>{linked(open)?.metadata?.institution_name||'—'}</b></div></div>}<div style={{marginTop:18,paddingTop:14,borderTop:'1px solid #F0F2F5',display:'flex',gap:10,color:'#667085',fontSize:9}}><CalendarDays size={15}/><span>Event date is tied to the underlying transaction when this message is transaction-linked.</span></div></aside></div>}</div></BankingShell>}

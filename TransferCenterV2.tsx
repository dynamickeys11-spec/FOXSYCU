import { useEffect, useMemo, useState } from 'react'
import { ArrowRight, Search } from 'lucide-react'
import { useNavigate, useSearchParams } from 'react-router-dom'
import { BankingShell } from './BankingShell'
import { useCustomerData } from './CustomerProvider'
import { supabase } from './supabaseClient'
import type { MoneyMovementRail } from './moneyMovementServiceV2'
import './feature-banking.css'

type Rail=MoneyMovementRail
type Destination={id:string;account_name:string;account_type:string;currency:string;account_number:string;synthetic_demo?:boolean;directory_destination_id?:string|null}
type InternationalDestination={id:string;recipient_name:string;country_code:'US'|'GB';country_name:string;institution_name:string;account_number:string|null;iban:string|null;routing_number:string|null;sort_code:string|null;swift_bic:string;destination_currency:string;synthetic_demo:boolean}
const rails:Array<{id:Rail;label:string}>= [{id:'internal',label:'FNCU account'},{id:'ach',label:'ACH'},{id:'wire',label:'Wire'},{id:'international',label:'International'},{id:'zelle_like',label:'Person'},{id:'deposit',label:'Deposit'}]
const normalizeMode=(mode:string|null):Rail=>{if(mode==='deposit')return'deposit';if(mode==='ach'||mode==='send')return'ach';if(mode==='wire')return'wire';if(mode==='international')return'international';if(mode==='p2p'||mode==='zelle_like')return'zelle_like';return'internal'}
const routing=(v:string)=>v?`${v.slice(0,3)}-${v.slice(3,6)}-${v.slice(6)}`:'—';const sortCode=(v:string)=>v?`${v.slice(0,2)}-${v.slice(2,4)}-${v.slice(4,6)}`:'—';const REVIEW_KEY='fncu:transfer-review-draft'
export function TransferCenterV2(){
 const{account,beneficiaries}=useCustomerData();const navigate=useNavigate();const[params]=useSearchParams();const[rail,setRail]=useState<Rail>(()=>normalizeMode(params.get('mode')))
 const[destinationNumber,setDestinationNumber]=useState('');const[selectedDestination,setSelectedDestination]=useState<Destination|null>(null);const[country,setCountry]=useState<'US'|'GB'>('US');const[internationalAccount,setInternationalAccount]=useState('');const[internationalRouting,setInternationalRouting]=useState('');const[internationalSwift,setInternationalSwift]=useState('');const[internationalDestination,setInternationalDestination]=useState<InternationalDestination|null>(null);const[recipientName,setRecipientName]=useState('');const[externalAccount,setExternalAccount]=useState('');const[externalRouting,setExternalRouting]=useState('');const[externalAccountType,setExternalAccountType]=useState('checking');const[p2pEmail,setP2pEmail]=useState('');const[amount,setAmount]=useState('');const[memo,setMemo]=useState('');const[lookupBusy,setLookupBusy]=useState(false);const[error,setError]=useState('')
 useEffect(()=>setRail(normalizeMode(params.get('mode'))),[params])
 useEffect(()=>{const id=params.get('beneficiary');if(!id)return;const b=(beneficiaries as any[]).find(x=>x.id===id);if(!b)return;setRail('ach');setRecipientName(String(b.name||''));setExternalAccount(String(b.account_number||''));setExternalRouting(String(b.routing_number||''));setExternalAccountType(String(b.account_type||'checking'))},[beneficiaries,params])
 const source=account?.id||'';const available=Number(account?.available_balance||0);const value=Number(amount)||0;const fee=rail==='wire'||rail==='international'?15:0;const emailValid=/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(p2pEmail.trim());const externalReady=rail==='zelle_like'?Boolean(recipientName.trim()&&emailValid):Boolean(recipientName.trim()&&/^\d{12}$/.test(externalAccount)&&/^\d{9}$/.test(externalRouting));const canContinue=useMemo(()=>value>0&&value+fee<=available&&(rail==='deposit'?!!source:rail==='internal'?!!source&&!!selectedDestination:rail==='international'?!!source&&!!internationalDestination:!!source&&externalReady),[value,fee,available,rail,source,selectedDestination,internationalDestination,externalReady])
 const reset=(next:Rail)=>{setRail(next);setError('');setSelectedDestination(null);setInternationalDestination(null)}
 const lookup=async()=>{const clean=destinationNumber.replace(/\D/g,'');if(clean.length!==12)return setError('Enter the 12-digit FNCU account number.');setLookupBusy(true);setError('');try{const{data,error:e}=await supabase.rpc('lookup_account_for_transfer',{p_account_number:clean});if(e)throw e;setSelectedDestination(data as Destination)}catch(e:any){setSelectedDestination(null);setError(e?.message||'Account not found.')}finally{setLookupBusy(false)}}
 const lookupInternational=async()=>{const id=country==='US'?internationalAccount.replace(/\D/g,''):internationalAccount.toUpperCase().replace(/\s/g,'');const r=internationalRouting.replace(/\D/g,'');const swift=internationalSwift.trim().toUpperCase();if(country==='US'&&(id.length!==12||r.length!==9))return setError('Enter a 12-digit account number and 9-digit routing number.');if(country==='GB'&&(id.length<15||r.length!==6))return setError('Enter a valid IBAN and 6-digit sort code.');if(!/^[A-Z0-9]{8}(?:[A-Z0-9]{3})?$/.test(swift))return setError('Enter a valid SWIFT/BIC.');setLookupBusy(true);setError('');try{const{data,error:e}=await supabase.rpc('lookup_international_transfer_destination',{p_country_code:country,p_account_identifier:id,p_routing_or_sort_code:r,p_swift_bic:swift});if(e)throw e;setInternationalDestination(data as InternationalDestination)}catch(e:any){setInternationalDestination(null);setError(e?.message||'Destination not recognized.')}finally{setLookupBusy(false)}}
 const continueToReview=()=>{if(!canContinue)return;sessionStorage.setItem(REVIEW_KEY,JSON.stringify({rail,sourceAccountId:source,amount:value,memo:memo.trim(),fee,destinationNumber,selectedDestination,country,internationalDestination,recipientName:recipientName.trim(),externalAccount,externalRouting,externalAccountType,p2pEmail:p2pEmail.trim().toLowerCase()}));navigate(`/transfers/review?rail=${rail}`)}
 const destinationName=internationalDestination?.recipient_name||selectedDestination?.account_name||recipientName||'Select destination';const destinationIdentifier=internationalDestination?.account_number||internationalDestination?.iban||selectedDestination?.account_number||externalAccount||p2pEmail||'—';const title=rail==='internal'?'Transfer between FNCU accounts':rail==='deposit'?'Deposit funds':rail==='international'?'International transfer':rail==='zelle_like'?'Pay a person':`${rail.toUpperCase()} transfer`
 return <BankingShell><div className="fb-page transfer-center-page"><header className="fb-heading"><span>MOVE MONEY</span><h1>{title}</h1><p>Enter the details below. You’ll review the transfer before authorization.</p></header><div className="fb-tabs transfer-rail-tabs" role="tablist" aria-label="Transfer type">{rails.map(r=><button key={r.id} role="tab" aria-selected={rail===r.id} className={rail===r.id?'active':''} onClick={()=>reset(r.id)}>{r.label}</button>)}</div><section className="fb-card transfer-entry-card"><div className="fb-card-head"><h2>Payment details</h2><p>Only the information needed for this transfer is required.</p></div>
 {rail==='internal'&&<><label className="fb-field">Destination account<input value={destinationNumber} onChange={e=>{setDestinationNumber(e.target.value.replace(/\D/g,'').slice(0,12));setSelectedDestination(null)}} inputMode="numeric" maxLength={12} placeholder="12-digit account number"/></label><button className="fb-btn" type="button" disabled={lookupBusy} onClick={()=>void lookup()}>{lookupBusy?'Checking…':<><Search size={15}/> Find account</>}</button>{selectedDestination&&<div className="fb-account-preview"><div><span>FNCU ACCOUNT</span><b>{selectedDestination.account_name}</b><small>{selectedDestination.account_type}</small></div><strong>•••• {selectedDestination.account_number.slice(-4)}</strong></div>}</>}
 {['ach','wire'].includes(rail)&&<><label className="fb-field">Recipient name<input value={recipientName} onChange={e=>setRecipientName(e.target.value)} placeholder="Full name"/></label><label className="fb-field">Account number<input value={externalAccount} onChange={e=>setExternalAccount(e.target.value.replace(/\D/g,'').slice(0,12))} inputMode="numeric" maxLength={12} placeholder="12-digit account number"/></label><label className="fb-field">Routing number<input value={externalRouting} onChange={e=>setExternalRouting(e.target.value.replace(/\D/g,'').slice(0,9))} inputMode="numeric" maxLength={9} placeholder="9-digit routing number"/></label><label className="fb-field">Account type<select value={externalAccountType} onChange={e=>setExternalAccountType(e.target.value)}><option value="checking">Checking</option><option value="savings">Savings</option></select></label></>}
 {rail==='zelle_like'&&<><label className="fb-field">Recipient name<input value={recipientName} onChange={e=>setRecipientName(e.target.value)} placeholder="Full name"/></label><label className="fb-field">Recipient email<input value={p2pEmail} onChange={e=>setP2pEmail(e.target.value)} type="email" inputMode="email" placeholder="recipient@example.com"/></label></>}
 {rail==='international'&&<><label className="fb-field">Destination country<select value={country} onChange={e=>{setCountry(e.target.value as 'US'|'GB');setInternationalDestination(null)}}><option value="US">United States</option><option value="GB">United Kingdom</option></select></label><label className="fb-field">{country==='US'?'Account number':'IBAN'}<input value={internationalAccount} onChange={e=>{setInternationalAccount(e.target.value);setInternationalDestination(null)}} placeholder={country==='US'?'12-digit account number':'GB IBAN'}/></label><label className="fb-field">{country==='US'?'Routing number':'Sort code'}<input value={internationalRouting} onChange={e=>{setInternationalRouting(e.target.value.replace(/\D/g,'').slice(0,country==='US'?9:6));setInternationalDestination(null)}} inputMode="numeric" placeholder={country==='US'?'9-digit routing number':'6-digit sort code'}/></label><label className="fb-field">SWIFT / BIC<input value={internationalSwift} onChange={e=>{setInternationalSwift(e.target.value.toUpperCase().slice(0,11));setInternationalDestination(null)}} maxLength={11} placeholder="8 or 11 characters"/></label><button className="fb-btn" type="button" disabled={lookupBusy} onClick={()=>void lookupInternational()}>{lookupBusy?'Checking…':<><Search size={15}/> Verify destination</>}</button>{internationalDestination&&<div className="fb-account-preview"><div><span>{internationalDestination.country_name.toUpperCase()}</span><b>{internationalDestination.recipient_name}</b><small>{internationalDestination.institution_name} · {internationalDestination.country_code==='US'?routing(internationalDestination.routing_number||''):sortCode(internationalDestination.sort_code||'')} · {internationalDestination.swift_bic}</small></div><strong>•••• {(internationalDestination.account_number||internationalDestination.iban||'').slice(-4)}</strong></div>}</>}
 <label className="fb-field">Amount<input value={amount} onChange={e=>setAmount(e.target.value.replace(/[^0-9.]/g,''))} inputMode="decimal" placeholder="0.00"/></label><label className="fb-field">Memo <span style={{fontWeight:400,color:'#667085'}}>(optional)</span><input value={memo} onChange={e=>setMemo(e.target.value)} placeholder="Add a note"/></label><div className="fb-actions"><button className="fb-btn primary" disabled={!canContinue} onClick={continueToReview}>Review transfer <ArrowRight size={15}/></button></div>{error&&<p className="fb-error">{error}</p>}</section><section className="fb-card" style={{marginTop:14}}><div className="fb-detail"><span>From</span><b>{account?.account_name||'Checking'}<small>•••• {account?.account_number_last4||'----'}</small></b></div><div className="fb-detail"><span>Available</span><b>${available.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div><div className="fb-detail"><span>Transfer fee</span><b>${fee.toFixed(2)}</b></div></section></div></BankingShell>
}
